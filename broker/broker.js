const SUPABASE_URL='https://sowdiflodjxxqrarbisi.supabase.co';
const SUPABASE_KEY='sb_publishable_3URkcBfqIZmtujzRnO1a1g_Xdadmjvt';
const db=supabase.createClient(SUPABASE_URL,SUPABASE_KEY);window.mightDb=db;
const $=id=>document.getElementById(id);let loads=[],currentUser=null,currentProfile=null;
const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const money=v=>Number(v||0).toLocaleString('en-US',{style:'currency',currency:'USD'});
const fmtDate=v=>{if(!v)return'—';const d=new Date(/^\d{4}-\d{2}-\d{2}$/.test(String(v))?`${v}T00:00:00`:v);return Number.isNaN(d.getTime())?'—':new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',year:'numeric'}).format(d)};
const statusLabel=v=>String(v||'').replace(/_/g,' ');
function showLogin(){$('app').classList.add('hidden');$('loginView').classList.remove('hidden')}
function addOverviewStyles(){if($('brokerOverviewStyles'))return;const s=document.createElement('style');s.id='brokerOverviewStyles';s.textContent=`
.overview{margin-bottom:28px}.overview-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin:18px 0}.overview-card{appearance:none;text-align:left;background:#fff;border:1px solid #e5e9ef;border-radius:15px;padding:18px;cursor:pointer;transition:transform .22s ease,box-shadow .22s ease,border-color .22s ease;animation:overviewIn .45s ease both}.overview-card:hover{transform:translateY(-3px);box-shadow:0 14px 34px rgba(18,35,55,.1);border-color:#cbd4df}.overview-card:active{transform:translateY(0) scale(.99)}.overview-card .ov-label{font-size:11px;letter-spacing:.1em;color:#7b8797;font-weight:800}.overview-card .ov-value{display:block;font-size:29px;line-height:1.1;margin-top:7px}.overview-card .ov-meta{display:block;color:#66768a;font-size:12px;margin-top:7px}.overview-row{display:grid;grid-template-columns:1.25fr .75fr;gap:14px}.overview-panel{background:#fff;border:1px solid #e5e9ef;border-radius:16px;padding:20px;animation:overviewIn .55s ease both}.overview-panel h3{margin:3px 0 5px}.overview-panel p{margin:0;color:#66768a;font-size:13px}.ov-bars{display:flex;align-items:flex-end;gap:10px;height:190px;margin-top:20px}.ov-bar-wrap{flex:1;height:100%;display:flex;flex-direction:column;justify-content:flex-end;align-items:center;gap:7px;cursor:pointer;background:transparent;border:0}.ov-bar{width:min(46px,72%);border-radius:8px 8px 3px 3px;background:#172033;min-height:8px;transition:height .5s cubic-bezier(.2,.8,.2,1),opacity .2s,transform .2s}.ov-bar-wrap:hover .ov-bar{transform:translateY(-3px);opacity:.8}.ov-bar-label{font-size:11px;color:#66768a;text-align:center}.ov-bar-value{font-size:11px;font-weight:800}.ov-donut{width:190px;height:190px;margin:18px auto 8px;position:relative;border-radius:50%}.ov-donut:after{content:'';position:absolute;inset:30px;border-radius:50%;background:#fff}.ov-donut-center{position:absolute;inset:0;z-index:2;display:grid;place-items:center;text-align:center;font-size:24px;font-weight:800}.ov-donut-center small{display:block;font-weight:500;color:#66768a;font-size:11px}.ov-legend{display:grid;gap:8px}.ov-legend button{display:flex;justify-content:space-between;align-items:center;background:transparent;border:0;padding:7px;border-radius:8px;color:#172033;font:inherit;cursor:pointer}.ov-legend button:hover{background:#f5f7fa}.ov-modal{position:fixed;inset:0;z-index:100;background:rgba(9,20,33,.46);display:grid;place-items:center;padding:20px;opacity:0;pointer-events:none;transition:opacity .2s ease}.ov-modal.open{opacity:1;pointer-events:auto}.ov-modal-card{width:min(800px,100%);max-height:82vh;overflow:auto;background:#fff;border-radius:18px;border:1px solid #e5e9ef;box-shadow:0 30px 90px rgba(0,0,0,.18);transform:translateY(12px) scale(.98);transition:transform .25s ease}.ov-modal.open .ov-modal-card{transform:none}.ov-modal-head{display:flex;justify-content:space-between;align-items:start;padding:22px 24px;border-bottom:1px solid #e5e9ef}.ov-modal-head h2{margin:4px 0}.ov-modal-body{padding:24px}.ov-list{display:grid;gap:9px}.ov-list button{background:#f7f9fb;border:1px solid #e7ebf0;border-radius:11px;padding:12px;text-align:left;cursor:pointer}.ov-list button:hover{background:#eef2f6}.ov-list .sub{display:block;color:#66768a;font-size:12px;margin-top:3px}@keyframes overviewIn{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}@media(max-width:900px){.overview-grid{grid-template-columns:1fr 1fr}.overview-row{grid-template-columns:1fr}}@media(max-width:600px){.overview-grid{grid-template-columns:1fr}}
`;document.head.appendChild(s)}
async function profile(){const{data:{user},error}=await db.auth.getUser();if(error)throw error;if(!user)return null;const{data:p,error:pe}=await db.from('employee_profiles').select('id,full_name,email,access_level,is_active').eq('id',user.id).maybeSingle();if(pe)throw pe;return{user,profile:p}}
async function signIn(e){e.preventDefault();$('loginError').textContent='';const{error}=await db.auth.signInWithPassword({email:$('email').value.trim(),password:$('password').value});if(error){$('loginError').textContent=error.message;return}await boot()}
async function boot(){try{const r=await profile();if(!r){showLogin();return}currentUser=r.user;currentProfile=r.profile;if(!currentProfile||currentProfile.access_level!=='broker'||!currentProfile.is_active){$('loginError').textContent='This portal is restricted to active broker accounts.';await db.auth.signOut();showLogin();return}$('userName').textContent=currentProfile.full_name||currentUser.email||'Broker';$('loginView').classList.add('hidden');$('app').classList.remove('hidden');addOverviewStyles();await loadLoads();buildOverview()}catch(e){console.error(e);$('loginError').textContent='Unable to load your broker profile. Please try again.';showLogin()}}
async function loadLoads(){const rows=$('rows');rows.innerHTML='<tr><td colspan="8" class="empty">Loading your loads…</td></tr>';const{data,error}=await db.from('loads').select('*').order('created_at',{ascending:false});if(error){rows.innerHTML=`<tr><td colspan="8" class="empty">Could not load your loads.<br><small>${esc(error.message)}</small></td></tr>`;return}loads=data||[];render();stats()}
function stats(){const active=loads.filter(l=>!['delivered','invoiced','paid','cancelled'].includes(l.status)).length;$('statActive').textContent=active;$('statBooked').textContent=loads.filter(l=>l.status==='booked').length;$('statTransit').textContent=loads.filter(l=>['dispatched','picked_up','in_transit'].includes(l.status)).length;$('statDelivered').textContent=loads.filter(l=>['delivered','invoiced','paid'].includes(l.status)).length}
function render(){const term=$('search').value.trim().toLowerCase(),filter=$('status').value;const filtered=loads.filter(l=>{if(filter!=='all'&&l.status!==filter)return false;if(!term)return true;return[l.load_number,l.origin,l.destination,l.equipment,l.carrier_name,l.carrier_mc,l.driver_name].some(v=>String(v||'').toLowerCase().includes(term))});if(!filtered.length){$('rows').innerHTML='<tr><td colspan="8" class="empty">No loads match your filters.</td></tr>';return}$('rows').innerHTML=filtered.map(l=>`<tr><td><strong>${esc(l.load_number)}</strong><span class="sub">Created ${fmtDate(l.created_at)}</span></td><td class="lane">${esc(l.origin)}<span>→</span>${esc(l.destination)}</td><td>${fmtDate(l.pickup_date)}</td><td>${esc(l.equipment||'—')}</td><td>${esc(l.carrier_name||'Unassigned')}<span class="sub">${esc(l.carrier_mc||'')}</span></td><td><span class="status">${esc(statusLabel(l.status))}</span></td><td>${l.customer_rate!=null?money(l.customer_rate):'—'}</td><td><button class="outline view" data-id="${esc(l.id)}">View</button></td></tr>`).join('');document.querySelectorAll('.view').forEach(b=>b.addEventListener('click',()=>openLoad(b.dataset.id)))}
function detail(label,value,full=false){return`<div class="detail ${full?'full':''}"><span>${label}</span><strong>${value||'—'}</strong></div>`}
function openLoad(id){const l=loads.find(x=>String(x.id)===String(id));if(!l)return;$('drawerTitle').textContent=l.load_number||'Load';$('drawerBody').innerHTML=`<div class="notice">This record is available because it belongs to your broker directory. Other brokers cannot retrieve it through the database.</div><div class="grid">${detail('Origin',esc(l.origin))}${detail('Destination',esc(l.destination))}${detail('Pickup',fmtDate(l.pickup_date))}${detail('Delivery',fmtDate(l.delivery_date))}${detail('Equipment',esc(l.equipment))}${detail('Commodity',esc(l.commodity))}${detail('Weight',l.weight?`${Number(l.weight).toLocaleString('en-US')} lbs`:'—')}${detail('Status',`<span class="status">${esc(statusLabel(l.status))}</span>`)}${detail('Customer Rate',l.customer_rate!=null?money(l.customer_rate):'—')}${detail('Carrier Rate',l.carrier_rate!=null?money(l.carrier_rate):'—')}${detail('Gross Margin',l.gross_margin!=null?money(l.gross_margin):'—')}${detail('Carrier',esc(l.carrier_name))}${detail('MC',esc(l.carrier_mc))}${detail('Driver',esc(l.driver_name))}${detail('Driver Phone',esc(l.driver_phone))}${detail('Truck #',esc(l.truck_number))}${detail('Trailer #',esc(l.trailer_number))}${detail('Pickup Window',esc([l.pickup_time_from,l.pickup_time_to].filter(Boolean).join(' – ')))}${detail('Delivery Window',esc([l.delivery_time_from,l.delivery_time_to].filter(Boolean).join(' – ')))}${detail('Special Requirements',esc(l.special_requirements),true)}${detail('Driver Instructions',esc(l.driver_instructions),true)}</div>`;$('drawer').classList.remove('hidden');$('drawer').setAttribute('aria-hidden','false')}
function closeDrawer(){$('drawer').classList.add('hidden');$('drawer').setAttribute('aria-hidden','true')}
function buildOverview(){let o=$('brokerOverview');if(!o){o=document.createElement('section');o.id='brokerOverview';o.className='overview';o.innerHTML='<div class="head"><div><div class="eyebrow">PERFORMANCE OVERVIEW</div><h1>Operations Overview</h1><p class="muted">A live summary of your assigned freight activity.</p></div></div><div id="overviewCards" class="overview-grid"></div><div class="overview-row"><section class="overview-panel"><div class="eyebrow">LOAD PIPELINE</div><h3>Loads by status</h3><p>Click any bar to inspect the loads behind the number.</p><div id="statusChart" class="ov-bars"></div></section><section class="overview-panel"><div class="eyebrow">LOAD MIX</div><h3>Current load mix</h3><p>Click a segment or legend item for details.</p><div id="mixChart"></div></section></div>';document.querySelector('.main').insertBefore(o,document.querySelector('.main .panel'));document.querySelector('.stats')?.classList.add('hidden')}const active=loads.filter(l=>!['delivered','invoiced','paid','cancelled'].includes(l.status));const booked=loads.filter(l=>l.status==='booked');const transit=loads.filter(l=>['dispatched','picked_up','in_transit'].includes(l.status));const delivered=loads.filter(l=>['delivered','invoiced','paid'].includes(l.status));const revenue=loads.reduce((a,l)=>a+Number(l.customer_rate||0),0);const margin=loads.reduce((a,l)=>a+Number(l.gross_margin||0),0);const cards=[['ACTIVE',active.length,'Loads requiring active work','active'],['BOOKED',booked.length,'Confirmed shipments','booked'],['IN TRANSIT',transit.length,'Dispatched / picked up / moving','transit'],['REVENUE',money(revenue),'Customer-rate value in your directory','revenue'],['GROSS MARGIN',money(margin),`${revenue?(margin/revenue*100).toFixed(1):'0.0'}% blended margin`,'margin'],['DELIVERED',delivered.length,'Delivered / invoiced / paid','delivered']];$('overviewCards').innerHTML=cards.map((c,i)=>`<button class="overview-card" style="animation-delay:${i*55}ms" data-ov="${c[3]}"><span class="ov-label">${c[0]}</span><strong class="ov-value">${c[1]}</strong><span class="ov-meta">${c[2]}</span></button>`).join('');$('overviewCards').querySelectorAll('[data-ov]').forEach(b=>b.addEventListener('click',()=>openOverviewDetail(b.dataset.ov)));renderStatusChart();renderMixChart()}
function renderStatusChart(){const defs=[['New','new'],['Booked','booked'],['Carrier','carrier_assigned'],['Dispatched','dispatched'],['Picked up','picked_up'],['In transit','in_transit'],['Delivered','delivered']];const vals=defs.map(d=>({label:d[0],key:d[1],value:loads.filter(l=>l.status===d[1]).length}));const max=Math.max(1,...vals.map(x=>x.value));$('statusChart').innerHTML=vals.map(v=>`<button class="ov-bar-wrap" data-chart-status="${v.key}" title="View ${v.label} loads"><span class="ov-bar-value">${v.value}</span><span class="ov-bar" style="height:${Math.max(6,(v.value/max)*145)}px"></span><span class="ov-bar-label">${v.label}</span></button>`).join('');$('statusChart').querySelectorAll('[data-chart-status]').forEach(b=>b.addEventListener('click',()=>openOverviewDetail('status',b.dataset.chartStatus)))}
function renderMixChart(){const cats=[['Active',loads.filter(l=>!['delivered','invoiced','paid','cancelled'].includes(l.status)).length],['Delivered',loads.filter(l=>['delivered','invoiced','paid'].includes(l.status)).length],['Cancelled',loads.filter(l=>l.status==='cancelled').length]];const total=Math.max(1,cats.reduce((a,x)=>a+x[1],0));let angle=0;const stops=cats.map(c=>{const st=angle;angle+=c[1]/total*360;return`#172033 ${st}deg ${angle}deg`}).join(',');$('mixChart').innerHTML=`<div class="ov-donut" style="background:conic-gradient(${stops})"><div class="ov-donut-center">${loads.length}<small>Total loads</small></div></div><div class="ov-legend">${cats.map((c,i)=>`<button data-mix="${i}"><span>${c[0]}</span><strong>${c[1]}</strong></button>`).join('')}</div>`;$('mixChart').querySelectorAll('[data-mix]').forEach(b=>b.addEventListener('click',()=>openOverviewDetail('mix',Number(b.dataset.mix))))}
function openOverviewDetail(type,value){let title='',subset=[];if(type==='active'){title='Active Loads';subset=loads.filter(l=>!['delivered','invoiced','paid','cancelled'].includes(l.status))}else if(type==='booked'){title='Booked Loads';subset=loads.filter(l=>l.status==='booked')}else if(type==='transit'){title='Loads In Transit';subset=loads.filter(l=>['dispatched','picked_up','in_transit'].includes(l.status))}else if(type==='delivered'){title='Delivered Loads';subset=loads.filter(l=>['delivered','invoiced','paid'].includes(l.status))}else if(type==='revenue'||type==='margin'){title=type==='revenue'?'Revenue Detail':'Gross Margin Detail';subset=loads}else if(type==='status'){title=`${statusLabel(value)} Loads`;subset=loads.filter(l=>l.status===value)}else if(type==='mix'){const cats=[['Active',l=>!['delivered','invoiced','paid','cancelled'].includes(l.status)],['Delivered',l=>['delivered','invoiced','paid'].includes(l.status)],['Cancelled',l=>l.status==='cancelled']];title=`${cats[value][0]} Loads`;subset=loads.filter(cats[value][1])}const modal=document.createElement('div');modal.className='ov-modal';modal.innerHTML=`<div class="ov-modal-card"><div class="ov-modal-head"><div><div class="eyebrow">DETAIL VIEW</div><h2>${esc(title)}</h2><div class="muted">${subset.length} record${subset.length===1?'':'s'}</div></div><button class="close" type="button">×</button></div><div class="ov-modal-body">${subset.length?`<div class="ov-list">${subset.slice(0,100).map(l=>`<button type="button" data-id="${esc(l.id)}"><strong>${esc(l.load_number||'Load')}</strong><span class="sub">${esc(l.origin||'')} → ${esc(l.destination||'')} • ${esc(statusLabel(l.status))}${l.customer_rate!=null?` • ${money(l.customer_rate)}`:''}</span></button>`).join('')}</div>`:'<div class="empty">No loads in this category.</div>'}</div></div>`;document.body.appendChild(modal);requestAnimationFrame(()=>modal.classList.add('open'));modal.querySelector('.close').addEventListener('click',()=>modal.remove());modal.addEventListener('click',e=>{if(e.target===modal)modal.remove()});modal.querySelectorAll('[data-id]').forEach(b=>b.addEventListener('click',()=>{modal.remove();openLoad(b.dataset.id)}))}
$('loginForm').addEventListener('submit',signIn);$('signOut').addEventListener('click',async()=>{await db.auth.signOut();showLogin()});$('refresh').addEventListener('click',async()=>{await loadLoads();buildOverview()});$('search').addEventListener('input',render);$('status').addEventListener('change',render);$('close').addEventListener('click',closeDrawer);$('backdrop').addEventListener('click',closeDrawer);db.auth.onAuthStateChange(e=>{if(e==='SIGNED_OUT')showLogin()});boot();
