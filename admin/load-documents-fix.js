(function(){const db=window.mightDb;if(!db)return;const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));function ensure(){const g=document.querySelector('#loadForm .loads-form-grid');if(!g||$('loadDocumentsSection'))return;const s=document.createElement('div');s.id='loadDocumentsSection';s.className='loads-form-section';s.style.gridColumn='1/-1';s.innerHTML='<div style="display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap"><div><h4>Documents</h4><p class="loads-section-note">Rate confirmations, BOLs, PODs and freight pictures stay attached to this load.</p></div><label class="outline" style="display:inline-flex;align-items:center;cursor:pointer;padding:8px 12px"><span>+ Upload Document</span><input id="loadDocumentInput" type="file" accept="application/pdf,image/jpeg,image/png,image/webp" multiple style="display:none"></label></div><div id="loadDocumentsList" style="display:grid;gap:8px;margin-top:10px"></div>';g.appendChild(s);$('loadDocumentInput').addEventListener('change',upload)}async function upload(e){const id=$('loadForm')?.dataset.id;if(!id){alert('Save the load before uploading documents.');e.target.value='';return}try{for(const file of [...(e.target.files||[])]){const safe=file.name.replace(/[^a-zA-Z0-9._-]+/g,'_'),path=`${id}/${Date.now()}_${safe}`;const {error:u}=await db.storage.from('load-documents').upload(path,file,{contentType:file.type||'application/octet-stream'});if(u)throw u;const {data:{user}}=await db.auth.getUser();const {error:i}=await db.from('load_documents').insert({load_id:id,document_type:file.type==='application/pdf'?'pdf':'image',file_name:file.name,mime_type:file.type,file_size:file.size,storage_path:path,created_by:user?.id||null});if(i)throw i}e.target.value='';loadDocs(id)}catch(err){console.error(err);alert(`Upload failed: ${err.message||err}`)}}async function loadDocs(id){const list=$('loadDocumentsList');if(!list)return;const {data,error}=await db.from('load_documents').select('id,file_name,document_type,file_size,storage_path,created_at').eq('load_id',id).order('created_at',{ascending:false});if(error){list.innerHTML='<div>Could not load documents.</div>';return}if(!data?.length){list.innerHTML='<div style="border:1px dashed #cfd6df;border-radius:8px;padding:10px;color:#667085;font-size:12px">No documents attached.</div>';return}list.innerHTML=data.map(d=>`<div style="display:flex;justify-content:space-between;align-items:center;border:1px solid #e1e6ed;border-radius:9px;padding:9px 11px;gap:10px"><div><strong>${esc(d.file_name)}</strong><small style="display:block;color:#667085;margin-top:3px">${esc(d.document_type)}${d.file_size?' • '+Math.round(d.file_size/1024)+' KB':''}</small></div><button type="button" class="outline load-doc-view" data-path="${esc(d.storage_path)}">View</button></div>`).join('');list.querySelectorAll('.load-doc-view').forEach(b=>b.onclick=async()=>{const {data:s,error:e}=await db.storage.from('load-documents').createSignedUrl(b.dataset.path,3600);if(e)alert(e.message);else window.open(s.signedUrl,'_blank','noopener,noreferrer')})}function init(){ensure();setInterval(()=>{ensure();const id=$('loadForm')?.dataset.id;if(id&&!$('loadModal')?.classList.contains('hidden'))loadDocs(id)},1500)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();})();