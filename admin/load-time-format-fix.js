(function(){function fmt(v){let s=String(v||'').trim().toUpperCase().replace(/\s+/g,'');const m=s.match(/^(\d{1,2})(?::?(\d{2}))?(AM|PM)$/);if(!m)return null;const h=+m[1],min=+(m[2]||0);if(h<1||h>12||min>59)return null;return `${h}:${String(min).padStart(2,'0')} ${m[3]}`}function init(){['load_pickupTimeFrom','load_pickupTimeTo','load_deliveryTimeFrom','load_deliveryTimeTo'].forEach(id=>{const x=document.getElementById(id);if(!x||x.dataset.instantTimeFmt)return;x.dataset.instantTimeFmt='1';x.addEventListener('input',()=>{const f=fmt(x.value);if(f&&x.value!==f){x.value=f;x.setSelectionRange(x.value.length,x.value.length)}})})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();setInterval(init,500)})();