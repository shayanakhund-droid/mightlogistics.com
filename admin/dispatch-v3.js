(function(){
  if(window.mightDispatchV3Loaded)return; window.mightDispatchV3Loaded=true;
  const db=window.mightDb;
  const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
  const money=v=>Number(v||0).toLocaleString('en-US',{style:'currency',currency:'USD'});
  const date=v=>v?new Date(v).toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'}):'—';
  const norm=v=>String(v??'').toLowerCase();
  let clients=[],trucks=[],loads=[],dispatchers=[],payments=[],assignments=[];
  let currentView='overview', period='this_month';
  const periods={this_month:'This month',last_month:'Last month',this_quarter:'This quarter',last_6_months:'Last 6 months',this_year:'This year',last_year:'Last year'};
  const statuses=['booked','picked_up','in_transit','delivered','invoiced','paid','cancelled'];
  function bounds(){const n=new Date();let s,e;if(period==='last_month'){s=new Date(n.getFullYear(),n.getMonth()-1,1);e=new Date(n.getFullYear(),n.getMonth(),1)}else if(period==='this_quarter'){const q=Math.floor(n.getMonth()/3)*3;s=new Date(n.getFullYear(),q,1);e=new Date(n.getFullYear(),q+3,1)}else if(period==='last_6_months'){s=new Date(n.getFullYear(),n.getMonth()-5,1);e=new Date(n.getFullYear(),n.getMonth()+1,1)}else if(period==='this_year'){s=new Date(n.getFullYear(),0,1);e=new Date(n.getFullYear()+1,0,1)}else if(period==='last_year'){s=new Date(n.getFullYear()-1,0,1);e=new Date(n.getFullYear(),0,1)}else{s=new Date(n.getFullYear(),n.getMonth(),1);e=new Date(n.getFullYear(),n.getMonth()+1,1)}return[s,e]}
  function inPeriod(v){if(!v)return true;const [s,e]=bounds(),d=new Date(v);return d>=s&&d<e}
  function clientName(id){return clients.find(x=>x.id===id)?.company_name||'Unassigned'}
  function truckName(id){return trucks.find(x=>x.id===id)?.unit_number||'—'}
  function dispatcherName(id){return dispatchers.find(x=>x.id===id)?.full_name||dispatchers.find(x=>x.id===id)?.email||'Unassigned'}
  function feeFor(l){return Number(l.dispatch_fee||0)}
  function showError(msg){console.error(msg);alert(msg)}
  async function q(table){try{const r=await db.from(table).select('*');if(r.error){console.warn(table,r.error.message);return []}return r.data||[]}catch(e){console.warn(table,e);return []}}
  async function loadData(){[clients,trucks,loads,dispatchers,payments,assignments]=await Promise.all([q('dispatch_clients'),q('dispatch_trucks'),q('dispatch_loads'),q('dispatchers'),q('dispatch_payments'),q('dispatch_assignments')]);renderAll()}
  function nav(){const n=document.querySelector('nav');if(!n)return;const old=n.querySelector('[data-section="dispatch"]');if(old)old.remove();const a=document.createElement('a');a.href='#dispatch';a.dataset.section='dispatch';a.textContent='Dispatch';n.appendChild(a)}
  function mount(){nav();let s=document.getElementById('dispatch');if(!s){const main=document.querySelector('main.main');if(!main)return;s=document.createElement('section');s.id='dispatch';s.className='content hidden';main.appendChild(s)}s.innerHTML=`<div class="dv3-wrap"><div class="dv3-head"><div><div class="kicker">DISPATCH OPERATIONS</div><h1 id="dv3Title">Dispatch Overview</h1><p id="dv3Subtitle">Run your dispatch operation from one workspace: clients, fleet, accepted loads, team and billing.</p></div><div class="dv3-actions"><select id="dv3Period">${Object.entries(periods).map(([k,v])=>`<option value="${k}">${v}</option>`).join('')}</select><button id="dv3Refresh" class="outline">↻ Refresh</button></div></div><div id="dv3Overview" class="dv3-view"></div><div id="dv3Clients" class="dv3-view hidden"></div><div id="dv3Fleet" class="dv3-view hidden"></div><div id="dv3Loads" class="dv3-view hidden"></div><div id="dv3Team" class="dv3-view hidden"></div><div id="dv3Payments" class="dv3-view hidden"></div></div>`;style();bind();loadData();setView(currentView)}
  function style(){if(document.getElementById('dv3Style'))return;const x=document.createElement('style');x.id='dv3Style';x.textContent=`#dispatch .dv3-wrap{max-width:1500px}.dv3-head{display:flex;justify-content:space-between;align-items:flex-end;gap:24px;margin-bottom:20px}.dv3-head h1{margin:4px 0;font-size:30px}.dv3-head p{margin:0;color:#657586;max-width:780px}.dv3-actions,.dv3-tools{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.dv3-actions select,.dv3-tools input,.dv3-tools select{border:1px solid #d5dfe8;border-radius:9px;background:#fff;padding:10px 12px;font:inherit}.dv3-view.hidden{display:none}.dv3-kpis{display:grid;grid-template-columns:repeat(6,1fr);gap:12px;margin-bottom:14px}.dv3-kpi{border:1px solid #dfe6ed;background:#fff;border-radius:14px;padding:16px;text-align:left;box-shadow:0 4px 15px #0d23380a}.dv3-kpi span{font-size:9px;letter-spacing:.13em;font-weight:800;color:#708197}.dv3-kpi b{display:block;font-size:25px;margin:7px 0;color:#176fbe}.dv3-kpi small{color:#7a8998;font-size:11px}.dv3-grid{display:grid;grid-template-columns:1.3fr 1fr;gap:14px}.dv3-panel{background:#fff;border:1px solid #dfe6ed;border-radius:14px;overflow:hidden;margin-bottom:14px;box-shadow:0 4px 16px #0d23380a}.dv3-panel>header{display:flex;justify-content:space-between;align-items:center;gap:14px;padding:18px 20px;border-bottom:1px solid #e9eef3}.dv3-panel h3{margin:3px 0}.dv3-panel header span{color:#7a8998;font-size:11px}.dv3-bars{display:flex;align-items:end;gap:12px;height:220px;padding:20px}.dv3-bar{flex:1;height:100%;display:flex;flex-direction:column;justify-content:flex-end;align-items:center;cursor:pointer}.dv3-bar i{width:min(58px,75%);background:#176fbe;border-radius:7px 7px 2px 2px;min-height:4px}.dv3-bar b{font-size:12px;margin-bottom:5px}.dv3-bar span{font-size:10px;color:#64748b;margin-top:7px}.dv3-rev{height:220px;display:flex;align-items:center;gap:22px;padding:0 24px}.dv3-ring{width:145px;height:145px;border-radius:50%;background:conic-gradient(#15966a var(--pct),#eef2f5 0);position:relative;flex:none}.dv3-ring:after{content:'';position:absolute;inset:28px;background:#fff;border-radius:50%}.dv3-legend{display:grid;gap:11px;font-size:12px}.dv3-legend div{display:grid;grid-template-columns:10px 1fr auto;gap:8px}.dv3-dot{width:9px;height:9px;border-radius:50%;background:#15966a}.dv3-dot.comm{background:#c77b17}.dv3-table{overflow:auto}.dv3-table table{width:100%;border-collapse:collapse;min-width:780px}.dv3-table th{text-align:left;padding:11px 16px;font-size:9px;text-transform:uppercase;letter-spacing:.1em;color:#788797;border-bottom:1px solid #e8edf2}.dv3-table td{padding:13px 16px;border-bottom:1px solid #edf1f4;color:#354556}.dv3-table tr:hover{background:#f7fbff}.dv3-status{font-size:9px;font-weight:800;text-transform:uppercase;padding:5px 8px;border-radius:99px;background:#edf2f6}.dv3-status.delivered,.dv3-status.paid,.dv3-status.active{background:#e7f7ef;color:#14744e}.dv3-status.cancelled,.dv3-status.inactive{background:#fdecec;color:#b42318}.dv3-status.in_transit,.dv3-status.picked_up{background:#e8f3fb;color:#176fbe}.dv3-empty{text-align:center;color:#7b8997;padding:30px!important}.dv3-statrow{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:14px}.dv3-smallstat{background:#fff;border:1px solid #dfe6ed;border-radius:12px;padding:14px}.dv3-smallstat span{font-size:9px;color:#7b8997;text-transform:uppercase;letter-spacing:.1em}.dv3-smallstat b{display:block;font-size:21px;margin-top:6px}.dv3-teamcards{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;padding:18px}.dv3-teamcard{border:1px solid #e3e9ef;border-radius:12px;padding:16px}.dv3-teamtop{display:flex;justify-content:space-between;align-items:start}.dv3-track{height:7px;background:#edf1f5;border-radius:99px;overflow:hidden;margin:12px 0}.dv3-track i{display:block;height:100%;background:#176fbe;border-radius:99px}.dv3-mini{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;font-size:11px;color:#6b7b8d}.dv3-mini b{display:block;color:#17253a;font-size:14px;margin-top:3px}.dv3-modal{position:fixed;inset:0;background:#071323d9;z-index:999;display:grid;place-items:center;padding:20px}.dv3-card{width:min(900px,100%);max-height:92vh;overflow:auto;background:#fff;border-radius:16px;box-shadow:0 30px 100px #0005}.dv3-card header{padding:20px 24px;border-bottom:1px solid #e5ebf0;display:flex;justify-content:space-between}.dv3-card h3{margin:3px 0}.dv3-form{padding:22px;display:grid;grid-template-columns:1fr 1fr;gap:13px}.dv3-form label{font-size:11px;font-weight:800;color:#354556}.dv3-form input,.dv3-form select,.dv3-form textarea{display:block;width:100%;box-sizing:border-box;margin-top:6px;border:1px solid #d5dfe8;border-radius:9px;padding:10px 11px;font:inherit}.dv3-form .full{grid-column:1/-1}.dv3-actions-row{grid-column:1/-1;display:flex;justify-content:flex-end;gap:8px}.dv3-close{border:0;background:transparent;font-size:26px;cursor:pointer}.dv3-link{border:0;background:none;color:#176fbe;cursor:pointer;font-weight:700}.dv3-badge{font-size:9px;font-weight:800;text-transform:uppercase;padding:5px 8px;border-radius:99px;background:#edf2f6}.dv3-rowactions{display:flex;gap:6px}.dv3-rowactions button{font-size:11px}.dv3-note{padding:14px 18px;background:#f7fafc;border-bottom:1px solid #e8edf2;color:#64748b;font-size:12px}@media(max-width:1200px){.dv3-kpis{grid-template-columns:repeat(3,1fr)}.dv3-grid{grid-template-columns:1fr}.dv3-teamcards{grid-template-columns:1fr 1fr}}@media(max-width:800px){.dv3-head{flex-direction:column;align-items:stretch}.dv3-kpis,.dv3-statrow{grid-template-columns:1fr 1fr}.dv3-form{grid-template-columns:1fr}.dv3-form .full{grid-column:auto}.dv3-teamcards{grid-template-columns:1fr}}`;document.head.appendChild(x)}
  function panel(title,kicker,subtitle,tools,body){return `<section class="dv3-panel"><header><div><div class="kicker">${kicker}</div><h3>${title}</h3>${subtitle?`<span>${subtitle}</span>`:''}</div>${tools||''}</header>${body}</section>`}
  function renderOverview(){const periodLoads=loads.filter(l=>inPeriod(l.pickup_date||l.created_at));const booked=periodLoads.filter(l=>l.status!=='cancelled');const revenue=booked.reduce((a,l)=>a+feeFor(l),0),comm=booked.reduce((a,l)=>a+Number(l.dispatcher_commission||0),0);const outstanding=booked.filter(l=>l.status!=='paid').reduce((a,l)=>a+feeFor(l),0);const counts=Object.fromEntries(statuses.map(s=>[s,periodLoads.filter(l=>l.status===s).length]));const max=Math.max(1,...Object.values(counts));const pct=revenue?Math.round(((revenue-comm)/revenue)*100):100;const bars=statuses.map(s=>`<div class="dv3-bar" data-status="${s}"><b>${counts[s]}</b><i style="height:${Math.max(4,counts[s]/max*165)}px"></i><span>${s.replaceAll('_',' ')}</span></div>`).join('');const clientRows=clients.map(c=>{const ls=booked.filter(l=>l.client_id===c.id);const val=ls.reduce((a,l)=>a+Number(l.rate||0),0),rev=ls.reduce((a,l)=>a+feeFor(l),0);return {...c,ls,val,rev}}).filter(x=>x.ls.length).sort((a,b)=>b.val-a.val).slice(0,8).map(c=>`<tr><td><b>${esc(c.company_name)}</b></td><td>${trucks.filter(t=>t.client_id===c.id&&t.status==='active').length}</td><td>${c.ls.length}</td><td>${money(c.val)}</td><td>${money(c.rev)}</td><td>${money(c.ls.length?c.rev/c.ls.length:0)}</td></tr>`).join('')||`<tr><td colspan="6" class="dv3-empty">No dispatch activity for this period.</td></tr>`;const teamRows=dispatchers.map(d=>{const ls=booked.filter(l=>l.dispatcher_id===d.id);const val=ls.reduce((a,l)=>a+Number(l.rate||0),0),rev=ls.reduce((a,l)=>a+feeFor(l),0),com=ls.reduce((a,l)=>a+Number(l.dispatcher_commission||0),0);return `<tr><td><b>${esc(d.full_name||d.email||'Dispatcher')}</b></td><td>${trucks.filter(t=>t.dispatcher_id===d.id&&t.status==='active').length}</td><td>${ls.length}</td><td>${money(val)}</td><td>${money(rev)}</td><td>${money(com)}</td></tr>`}).join('')||`<tr><td colspan="6" class="dv3-empty">No dispatcher activity for this period.</td></tr>`;document.getElementById('dv3Overview').innerHTML=`<div class="dv3-kpis"><div class="dv3-kpi"><span>ACTIVE CLIENTS</span><b>${clients.filter(c=>c.status!=='inactive').length}</b><small>Dispatch accounts</small></div><div class="dv3-kpi"><span>ACTIVE TRUCKS</span><b>${trucks.filter(t=>t.status!=='inactive').length}</b><small>Client fleet</small></div><div class="dv3-kpi"><span>BOOKED LOADS</span><b>${booked.length}</b><small>${periods[period]}</small></div><div class="dv3-kpi"><span>DISPATCH REVENUE</span><b>${money(revenue)}</b><small>Fees earned</small></div><div class="dv3-kpi"><span>COMMISSIONS</span><b>${money(comm)}</b><small>Dispatcher earnings</small></div><div class="dv3-kpi"><span>OUTSTANDING</span><b>${money(outstanding)}</b><small>Unpaid dispatch fees</small></div></div><div class="dv3-grid">${panel('Booked loads by status','LOAD ACTIVITY','Click a status to inspect accepted loads.',null,`<div id="dv3Bars" class="dv3-bars">${bars}</div>`)}${panel('Dispatch revenue','REVENUE','Net dispatch revenue after dispatcher commissions.',null,`<div class="dv3-rev"><div class="dv3-ring" style="--pct:${pct}%"></div><div class="dv3-legend"><div><i class="dv3-dot"></i><span>Dispatch fees</span><b>${money(revenue)}</b></div><div><i class="dv3-dot comm"></i><span>Commissions</span><b>${money(comm)}</b></div><div><i></i><span>Net</span><b>${money(revenue-comm)}</b></div></div></div>`)}</div>${panel('Top dispatch clients','CLIENT PERFORMANCE','Clients ranked by accepted load value.',`<button class="outline" data-jump="clients">View clients</button>`,`<div class="dv3-table"><table><thead><tr><th>Client</th><th>Trucks</th><th>Loads</th><th>Gross load value</th><th>Dispatch revenue</th><th>Avg. fee</th></tr></thead><tbody>${clientRows}</tbody></table></div>`)}${panel('Dispatcher performance','TEAM PERFORMANCE','Load volume and revenue by dispatcher.',`<button class="outline" data-jump="team">View team</button>`,`<div class="dv3-table"><table><thead><tr><th>Dispatcher</th><th>Trucks</th><th>Loads</th><th>Load value</th><th>Revenue</th><th>Commission</th></tr></thead><tbody>${teamRows}</tbody></table></div>`)}`}
  function renderClients(){const search=norm(document.getElementById('dv3ClientSearch')?.value);const rows=clients.filter(c=>!search||[c.company_name,c.contact_name,c.email,c.phone,c.mc_number,c.dot_number].some(v=>norm(v).includes(search))).map(c=>{const ct=trucks.filter(t=>t.client_id===c.id).length;const fee=c.fee_type==='percentage'?`${c.fee_value||0}%`:`${money(c.fee_value||0)} / load`;return `<tr><td><b>${esc(c.company_name)}</b><div class="muted">${esc(c.notes||'')}</div></td><td>${esc(c.mc_number||'—')} / ${esc(c.dot_number||'—')}</td><td>${esc(c.contact_name||'—')}<br>${esc(c.email||'')}</td><td>${ct}</td><td>${fee}</td><td><span class="dv3-status ${esc(c.status||'active')}">${esc(c.status||'active')}</span></td><td><div class="dv3-rowactions"><button class="outline" data-edit-client="${c.id}">Edit</button></div></td></tr>`}).join('')||`<tr><td colspan="7" class="dv3-empty">No clients found.</td></tr>`;document.getElementById('dv3Clients').innerHTML=panel('Clients','DISPATCH CRM','Manage carrier clients, their fee plans and fleet relationships.',`<div class="dv3-tools"><input id="dv3ClientSearch" placeholder="Search company, MC, DOT"><button class="primary" id="dv3AddClient">+ Client</button></div>`,`<div class="dv3-table"><table><thead><tr><th>Company</th><th>MC / DOT</th><th>Contact</th><th>Trucks</th><th>Fee plan</th><th>Status</th><th></th></tr></thead><tbody>${rows}</tbody></table></div>`);document.getElementById('dv3ClientSearch').value=search}
  function renderFleet(){const search=norm(document.getElementById('dv3FleetSearch')?.value);const rows=trucks.filter(t=>!search||[t.unit_number,t.driver_name,t.equipment_type,t.truck_type,t.home_city,t.home_state,clientName(t.client_id)].some(v=>norm(v).includes(search))).map(t=>`<tr><td><b>${esc(t.unit_number||'—')}</b><div>${esc(t.truck_type||'')}</div></td><td>${esc(clientName(t.client_id))}</td><td>${esc(t.equipment_type||'—')}</td><td>${esc(t.driver_name||'—')}<br>${esc(t.driver_phone||'')}</td><td>${esc([t.home_city,t.home_state].filter(Boolean).join(', ')||'—')}</td><td>${esc(dispatcherName(t.dispatcher_id))}</td><td><span class="dv3-status ${esc(t.status||'active')}">${esc(t.status||'active')}</span></td><td><button class="outline" data-edit-truck="${t.id}">Edit</button></td></tr>`).join('')||`<tr><td colspan="8" class="dv3-empty">No fleet records found.</td></tr>`;document.getElementById('dv3Fleet').innerHTML=panel('Client Fleet','FLEET','Every truck assigned to a dispatch client.',`<div class="dv3-tools"><input id="dv3FleetSearch" placeholder="Search unit, driver, client"><button class="primary" id="dv3AddTruck">+ Truck</button></div>`,`<div class="dv3-table"><table><thead><tr><th>Unit</th><th>Client</th><th>Equipment</th><th>Driver</th><th>Home</th><th>Dispatcher</th><th>Status</th><th></th></tr></thead><tbody>${rows}</tbody></table></div>`);document.getElementById('dv3FleetSearch').value=search}
  function renderLoads(){const search=norm(document.getElementById('dv3LoadSearch')?.value),filter=document.getElementById('dv3LoadStatus')?.value||'all';const rows=loads.filter(l=>(filter==='all'||l.status===filter)&&(!search||[l.load_number,l.origin,l.destination,l.broker_name,l.board_reference,clientName(l.client_id),truckName(l.truck_id)].some(v=>norm(v).includes(search)))).sort((a,b)=>new Date(b.pickup_date||b.created_at)-new Date(a.pickup_date||a.created_at)).map(l=>`<tr><td><b>${esc(l.load_number||'No #')}</b><div>${esc(l.source_board||'')}</div></td><td>${esc(clientName(l.client_id))}<br><small>${esc(truckName(l.truck_id))}</small></td><td>${esc(l.origin||'—')} → ${esc(l.destination||'—')}</td><td>${date(l.pickup_date)}</td><td>${money(l.rate)}</td><td>${l.rate_per_mile?money(l.rate_per_mile):'—'}</td><td>${money(l.dispatch_fee)}</td><td>${money(l.dispatcher_commission)}</td><td><span class="dv3-status ${esc(l.status)}">${esc(l.status).replaceAll('_',' ')}</span></td><td><button class="outline" data-edit-load="${l.id}">Edit</button></td></tr>`).join('')||`<tr><td colspan="10" class="dv3-empty">No accepted loads found.</td></tr>`;document.getElementById('dv3Loads').innerHTML=panel('Accepted Loads','ACCEPTED LOADS','This is the dispatch financial and operational record. No rate confirmations are generated here.',`<div class="dv3-tools"><input id="dv3LoadSearch" placeholder="Search load, lane, client, broker"><select id="dv3LoadStatus"><option value="all">All statuses</option>${statuses.map(s=>`<option value="${s}">${s.replaceAll('_',' ')}</option>`).join('')}</select><button class="primary" id="dv3AddLoad">+ Accepted Load</button></div>`,`<div class="dv3-note">Track the booked rate, dispatcher fee, commission, status and broker information for every accepted load.</div><div class="dv3-table"><table><thead><tr><th>Load</th><th>Client / Truck</th><th>Lane</th><th>Pickup</th><th>Rate</th><th>RPM</th><th>Fee</th><th>Commission</th><th>Status</th><th></th></tr></thead><tbody>${rows}</tbody></table></div>`);document.getElementById('dv3LoadSearch').value=search;document.getElementById('dv3LoadStatus').value=filter}
  function renderTeam(){const rows=dispatchers.map(d=>{const activeTrucks=trucks.filter(t=>t.dispatcher_id===d.id&&t.status!=='inactive').length;const ls=loads.filter(l=>l.dispatcher_id===d.id&&l.status!=='cancelled');const rev=ls.reduce((a,l)=>a+feeFor(l),0),com=ls.reduce((a,l)=>a+Number(l.dispatcher_commission||0),0),cap=Math.min(100,activeTrucks/5*100);return `<div class="dv3-teamcard"><div class="dv3-teamtop"><div><b>${esc(d.full_name||d.email||'Dispatcher')}</b><div class="muted">${esc(d.email||'')}</div></div><span class="dv3-badge">${d.is_active===false?'Inactive':'Active'}</span></div><div class="dv3-track"><i style="width:${cap}%"></i></div><div class="muted">${activeTrucks}/5 active trucks</div><div class="dv3-mini" style="margin-top:14px"><div>Loads<b>${ls.length}</b></div><div>Revenue<b>${money(rev)}</b></div><div>Commission<b>${money(com)}</b></div></div></div>`}).join('')||`<div class="dv3-empty">No dispatcher records found.</div>`;document.getElementById('dv3Team').innerHTML=panel('Dispatchers','DISPATCH TEAM','Manage dispatcher capacity and review dispatcher performance.',`<button class="primary" id="dv3AddDispatcher">+ Dispatcher</button>`,`<div class="dv3-teamcards">${rows}</div>`)}
  function paymentRows(){const p=payments.length?payments:loads.filter(l=>l.dispatch_fee);return p.map(p=>{const fee=Number(p.amount||p.fee||p.dispatch_fee||0),paid=Number(p.paid_amount||p.paid||((p.status==='paid')?fee:0)),bal=Math.max(0,fee-paid);return `<tr><td><b>${esc(p.invoice_number||p.invoice||'—')}</b></td><td>${esc(clientName(p.client_id))}</td><td>${esc(p.load_number||loads.find(l=>l.id===p.load_id)?.load_number||'—')}</td><td>${money(fee)}</td><td>${money(paid)}</td><td>${money(bal)}</td><td>${date(p.due_date)}</td><td><span class="dv3-status ${bal<=0?'paid':(p.status||'outstanding')}">${bal<=0?'paid':esc(p.status||'outstanding')}</span></td></tr>`}).join('')||`<tr><td colspan="8" class="dv3-empty">No payment records yet. Accepted load fees will appear here.</td></tr>`}
  function renderPayments(){const total=loads.reduce((a,l)=>a+feeFor(l),0),paid=loads.filter(l=>l.status==='paid').reduce((a,l)=>a+feeFor(l),0),out=Math.max(0,total-paid);document.getElementById('dv3Payments').innerHTML=panel('Payments','DISPATCH BILLING','Track dispatch fees earned, collected and outstanding.',null,`<div class="dv3-statrow"><div class="dv3-smallstat"><span>Total billed</span><b>${money(total)}</b></div><div class="dv3-smallstat"><span>Collected</span><b>${money(paid)}</b></div><div class="dv3-smallstat"><span>Outstanding</span><b>${money(out)}</b></div><div class="dv3-smallstat"><span>Collection rate</span><b>${total?Math.round(paid/total*100):0}%</b></div></div><div class="dv3-table"><table><thead><tr><th>Invoice</th><th>Client</th><th>Load</th><th>Fee</th><th>Paid</th><th>Balance</th><th>Due</th><th>Status</th></tr></thead><tbody>${paymentRows()}</tbody></table></div>`)}
  function renderAll(){renderOverview();renderClients();renderFleet();renderLoads();renderTeam();renderPayments();setView(currentView)}
  function setView(view){currentView=view||'overview';const map={overview:'Overview',clients:'Clients',fleet:'Fleet',loads:'Accepted Loads',team:'Dispatchers',payments:'Payments'};document.querySelectorAll('#dispatch .dv3-view').forEach(e=>e.classList.add('hidden'));document.getElementById('dv3'+({overview:'Overview',clients:'Clients',fleet:'Fleet',loads:'Loads',team:'Team',payments:'Payments'}[currentView]))?.classList.remove('hidden');const t=document.getElementById('dv3Title');const sub=document.getElementById('dv3Subtitle');if(t)t.textContent=`Dispatch ${map[currentView]||'Overview'}`;if(sub)sub.textContent=currentView==='overview'?'Run your dispatch operation from one workspace: clients, fleet, accepted loads, team and billing.':({clients:'Manage carrier clients, fee plans and relationships.',fleet:'Manage every truck, driver and dispatcher assignment.',loads:'Record every accepted load and its financial outcome.',team:'Manage dispatcher capacity and performance.',payments:'Track dispatch fees, collections and outstanding balances.'}[currentView]||'Dispatch operations.');const w=document.getElementById('adminWorkspaceSwitcher');w?.querySelectorAll('[data-dview]').forEach(b=>b.classList.toggle('active',b.dataset.dview===currentView))}
  function modal(title,body,save){const m=document.createElement('div');m.className='dv3-modal';m.innerHTML=`<div class="dv3-card"><header><div><div class="kicker">DISPATCH</div><h3>${title}</h3></div><button type="button" class="dv3-close">×</button></header><form class="dv3-form">${body}<div class="dv3-actions-row"><button type="button" class="outline dv3-cancel">Cancel</button><button class="primary" type="submit">Save</button></div></form></div>`;document.body.appendChild(m);m.querySelector('.dv3-close').onclick=()=>m.remove();m.querySelector('.dv3-cancel').onclick=()=>m.remove();m.onclick=e=>{if(e.target===m)m.remove()};m.querySelector('form').onsubmit=async e=>{e.preventDefault();const ok=await save(new FormData(e.currentTarget));if(ok){m.remove();await loadData()}}}
  function clientModal(id){const c=id?clients.find(x=>x.id===id):null;modal(c?'Edit Client':'New Dispatch Client',`<label>Company Name<input name="company" value="${esc(c?.company_name)}" required></label><label>Contact Name<input name="contact" value="${esc(c?.contact_name)}"></label><label>Email<input name="email" type="email" value="${esc(c?.email)}"></label><label>Phone<input name="phone" value="${esc(c?.phone)}"></label><label>MC Number<input name="mc" value="${esc(c?.mc_number)}"></label><label>DOT Number<input name="dot" value="${esc(c?.dot_number)}"></label><label>Fee Type<select name="fee_type"><option value="percentage" ${c?.fee_type==='percentage'?'selected':''}>Percentage</option><option value="flat_per_load" ${c?.fee_type==='flat_per_load'?'selected':''}>Flat per load</option></select></label><label>Fee Value<input name="fee_value" type="number" step="0.01" value="${c?.fee_value??''}" required></label><label>Status<select name="status"><option value="active" ${c?.status!=='inactive'?'selected':''}>Active</option><option value="inactive" ${c?.status==='inactive'?'selected':''}>Inactive</option></select></label><label class="full">Notes<textarea name="notes">${esc(c?.notes)}</textarea></label>`,async f=>{const data={company_name:f.get('company'),contact_name:f.get('contact'),email:f.get('email'),phone:f.get('phone'),mc_number:f.get('mc'),dot_number:f.get('dot'),fee_type:f.get('fee_type'),fee_value:Number(f.get('fee_value')),status:f.get('status'),notes:f.get('notes')};const r=c?await db.from('dispatch_clients').update(data).eq('id',c.id):await db.from('dispatch_clients').insert(data);if(r.error){showError(r.error.message);return false}return true})}
  function truckModal(id){const t=id?trucks.find(x=>x.id===id):null;modal(t?'Edit Truck':'Add Truck',`<label>Client<select name="client" required>${clients.map(c=>`<option value="${c.id}" ${t?.client_id===c.id?'selected':''}>${esc(c.company_name)}</option>`).join('')}</select></label><label>Unit Number<input name="unit" value="${esc(t?.unit_number)}" required></label><label>Equipment Type<input name="equipment" value="${esc(t?.equipment_type)}"></label><label>Truck Type<input name="truck_type" value="${esc(t?.truck_type)}"></label><label>Year<input name="year" type="number" value="${t?.year??''}"></label><label>Make<input name="make" value="${esc(t?.make)}"></label><label>Model<input name="model" value="${esc(t?.model)}"></label><label>VIN<input name="vin" value="${esc(t?.vin)}"></label><label>Driver Name<input name="driver" value="${esc(t?.driver_name)}"></label><label>Driver Phone<input name="driver_phone" value="${esc(t?.driver_phone)}"></label><label>Home City<input name="city" value="${esc(t?.home_city)}"></label><label>Home State<input name="state" maxlength="2" value="${esc(t?.home_state)}"></label><label>Dispatcher<select name="dispatcher"><option value="">Unassigned</option>${dispatchers.map(d=>`<option value="${d.id}" ${t?.dispatcher_id===d.id?'selected':''}>${esc(d.full_name||d.email)}</option>`).join('')}</select></label><label>Status<select name="status"><option value="active" ${t?.status!=='inactive'?'selected':''}>Active</option><option value="inactive" ${t?.status==='inactive'?'selected':''}>Inactive</option></select></label>`,async f=>{const data={client_id:f.get('client'),unit_number:f.get('unit'),equipment_type:f.get('equipment'),truck_type:f.get('truck_type'),year:f.get('year')?Number(f.get('year')):null,make:f.get('make'),model:f.get('model'),vin:f.get('vin'),driver_name:f.get('driver'),driver_phone:f.get('driver_phone'),home_city:f.get('city'),home_state:f.get('state'),dispatcher_id:f.get('dispatcher')||null,status:f.get('status')};const r=t?await db.from('dispatch_trucks').update(data).eq('id',t.id):await db.from('dispatch_trucks').insert(data);if(r.error){showError(r.error.message);return false}return true})}
  function loadModal(id){const l=id?loads.find(x=>x.id===id):null;modal(l?'Edit Accepted Load':'Record Accepted Load',`<label>Client<select name="client" required>${clients.map(c=>`<option value="${c.id}" ${l?.client_id===c.id?'selected':''}>${esc(c.company_name)}</option>`).join('')}</select></label><label>Truck<select name="truck"><option value="">Unassigned</option>${trucks.filter(t=>t.status!=='inactive').map(t=>`<option value="${t.id}" ${l?.truck_id===t.id?'selected':''}>${esc(clientName(t.client_id))} — ${esc(t.unit_number)}</option>`).join('')}</select></label><label>Dispatcher<select name="dispatcher"><option value="">Unassigned</option>${dispatchers.filter(d=>d.is_active!==false).map(d=>`<option value="${d.id}" ${l?.dispatcher_id===d.id?'selected':''}>${esc(d.full_name||d.email)}</option>`).join('')}</select></label><label>Source Board<select name="board"><option>DAT</option><option>Truckstop</option><option>Other</option></select></label><label>Board Reference<input name="ref" value="${esc(l?.board_reference)}"></label><label>Load Number<input name="number" value="${esc(l?.load_number)}"></label><label>Origin<input name="origin" value="${esc(l?.origin)}" required></label><label>Destination<input name="destination" value="${esc(l?.destination)}" required></label><label>Pickup Date<input name="pickup" type="date" value="${esc(l?.pickup_date)}"></label><label>Delivery Date<input name="delivery" type="date" value="${esc(l?.delivery_date)}"></label><label>Equipment<input name="equipment" value="${esc(l?.equipment)}"></label><label>Commodity<input name="commodity" value="${esc(l?.commodity)}"></label><label>Miles<input name="miles" type="number" step="0.1" value="${l?.miles??''}"></label><label>Accepted Rate<input name="rate" type="number" step="0.01" value="${l?.rate??''}" required></label><label>Broker Name<input name="broker" value="${esc(l?.broker_name)}"></label><label>Broker Phone<input name="broker_phone" value="${esc(l?.broker_phone)}"></label><label>Broker Email<input name="broker_email" type="email" value="${esc(l?.broker_email)}"></label><label>Status<select name="status">${statuses.map(s=>`<option value="${s}" ${l?.status===s?'selected':''}>${s.replaceAll('_',' ')}</option>`).join('')}</select></label><label class="full">Notes<textarea name="notes">${esc(l?.notes)}</textarea></label>`,async f=>{const rate=Number(f.get('rate')),miles=Number(f.get('miles')||0),client=clients.find(c=>c.id===f.get('client'));let fee=l?.dispatch_fee||0;if(client?.fee_type==='percentage')fee=rate*Number(client.fee_value||0)/100;else if(client?.fee_type==='flat_per_load')fee=Number(client.fee_value||0);const data={client_id:f.get('client'),truck_id:f.get('truck')||null,dispatcher_id:f.get('dispatcher')||null,source_board:f.get('board'),board_reference:f.get('ref'),load_number:f.get('number')||null,origin:f.get('origin'),destination:f.get('destination'),pickup_date:f.get('pickup')||null,delivery_date:f.get('delivery')||null,equipment:f.get('equipment'),commodity:f.get('commodity'),miles:miles||null,rate,rate_per_mile:miles?rate/miles:null,dispatch_fee:fee,broker_name:f.get('broker'),broker_phone:f.get('broker_phone'),broker_email:f.get('broker_email'),status:f.get('status'),notes:f.get('notes')};const r=l?await db.from('dispatch_loads').update(data).eq('id',l.id):await db.from('dispatch_loads').insert(data);if(r.error){showError(r.error.message);return false}return true})}
  function dispatcherModal(){modal('New Dispatcher',`<label>Full Name<input name="full_name" required></label><label>Email<input name="email" type="email" required></label><label>Status<select name="active"><option value="true">Active</option><option value="false">Inactive</option></select></label>`,async f=>{const data={full_name:f.get('full_name'),email:f.get('email'),is_active:f.get('active')==='true'};const r=await db.from('dispatchers').insert(data);if(r.error){showError(r.error.message);return false}return true})}
  function bind(){document.getElementById('dv3Refresh').onclick=loadData;document.getElementById('dv3Period').onchange=e=>{period=e.target.value;renderAll()};document.getElementById('dv3Period').value=period;document.getElementById('dv3Overview').addEventListener('click',e=>{const j=e.target.closest('[data-jump]');if(j)setView(j.dataset.jump);const b=e.target.closest('[data-status]');if(b){setView('loads');setTimeout(()=>{const s=document.getElementById('dv3LoadStatus');if(s){s.value=b.dataset.status;renderLoads()}},0)}});document.getElementById('dv3Clients').addEventListener('click',e=>{if(e.target.id==='dv3AddClient')clientModal();const b=e.target.closest('[data-edit-client]');if(b)clientModal(b.dataset.editClient)});document.getElementById('dv3Clients').addEventListener('input',e=>{if(e.target.id==='dv3ClientSearch')renderClients()});document.getElementById('dv3Fleet').addEventListener('click',e=>{if(e.target.id==='dv3AddTruck')truckModal();const b=e.target.closest('[data-edit-truck]');if(b)truckModal(b.dataset.editTruck)});document.getElementById('dv3Fleet').addEventListener('input',e=>{if(e.target.id==='dv3FleetSearch')renderFleet()});document.getElementById('dv3Loads').addEventListener('click',e=>{if(e.target.id==='dv3AddLoad')loadModal();const b=e.target.closest('[data-edit-load]');if(b)loadModal(b.dataset.editLoad)});document.getElementById('dv3Loads').addEventListener('input',e=>{if(e.target.id==='dv3LoadSearch')renderLoads()});document.getElementById('dv3Loads').addEventListener('change',e=>{if(e.target.id==='dv3LoadStatus')renderLoads()});document.getElementById('dv3Team').addEventListener('click',e=>{if(e.target.id==='dv3AddDispatcher')dispatcherModal()})}
  window.initMightDispatchV2=mount;window.mightDispatchV2={setView};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount);else setTimeout(mount,0);
})();
